<%= form_with(model: todo_task, local: true) do |form| %>
  <% if todo_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_task.errors.count, "error") %> prohibited this todo_task from being saved:</h2>

      <ul>
      <% todo_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="jumbotron">
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :complete %>
    <%= form.check_box :complete %>
  </div>


  <div class="field">

    <%= form.hidden_field :todo_list_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

</div>

